import React from 'react';

export default function OrgAlerts() {
  return (
    <div className="glass-card" style={{ padding: '1.25rem' }}>
      <h3 style={{ fontSize: '1rem', fontWeight: 800, color: 'var(--navy-deep)', marginBottom: '0.75rem' }}>
        ORGANIZATION ALERTS & NOTIFICATIONS
      </h3>
      <div style={{ fontSize: '0.85rem', color: 'var(--text-sub)' }}>
        Command center alerts, road blockage notifications, and emergency broadcasts.
      </div>
    </div>
  );
}
